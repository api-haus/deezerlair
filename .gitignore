# credentials
.env
state/

# Your preferences — which recording wins, how much to ask, playlist taste.
# Personal, so it must not collide with an upstream pull. The tracked template
# is PREFS.example.md.
PREFS.md

# rebuildable
cache/
snapshots/

__pycache__/
*.pyc

# The skill is part of what this repo ships, so it overrides a global ignore
# of .claude/ if one is set.
!.claude/
